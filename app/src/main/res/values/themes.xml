<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the whole UI; the window theme only needs to be edge-to-edge friendly. -->
    <style name="Theme.Showcase" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.Showcase.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Showcase</item>
    </style>
</resources>
